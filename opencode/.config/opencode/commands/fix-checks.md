---
description: Diagnose and repair lint, format, type, test, or build failures sequentially.
agent: debugger
subtask: false
---

Fix failing checks: $ARGUMENTS

Run or inspect exact failing command, group failures by root cause, fix smallest cause
first, rerun focused check, then original command. Never weaken checks or delete tests.

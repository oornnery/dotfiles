# Project guide template

Copy AGENTS.base.md only when a project needs persistent agent instructions.
Replace placeholders with verified paths, commands and non-obvious constraints.
Reuse existing project docs; no SPEC.md or .spec/.mem hierarchy is required.

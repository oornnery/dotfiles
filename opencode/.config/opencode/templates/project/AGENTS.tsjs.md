## TypeScript/JavaScript overlay

- Runtime: `{{runtime}}`; package manager: `{{npm|pnpm|yarn|bun}}`.
- Framework: `{{framework}}`; source: `{{path}}`; tests: `{{path}}`.
- Preserve strict TypeScript and established server/client boundaries.
- Reuse design system, schemas, API clients, and state patterns already present.
- Commands:
  - Focused test: `{{command}}`
  - Typecheck/lint: `{{command}}`
  - Production build: `{{command}}`

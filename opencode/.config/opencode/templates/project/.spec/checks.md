# Acceptance checks

| Requirement | Evidence/check | Status |
| --- | --- | --- |
| {{observable requirement}} | `{{command}}` or `path:line` | pending |

`PASS` requires current evidence. Record blocked checks with exact prerequisite;
never convert not-run into pass.

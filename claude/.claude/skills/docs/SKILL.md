---
description: Update documentation from verified code and configuration, never from assumptions.
argument-hint: [doc or area]
---

Update documentation for: $ARGUMENTS

- Read the code and configuration that the docs describe; document what is true now.
- Do not invent commands, flags, paths or outputs. Run a command before documenting it.
- Keep one source of truth per subject and cross-reference the rest.
- Leave unknowns as `TBD` rather than guessing.

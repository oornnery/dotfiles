---
description: Implement the requested task completely, with appropriate verification. Use for ordinary feature and fix work.
argument-hint: [task]
---

Implement this completely, then verify it: $ARGUMENTS

- Inspect the relevant code and follow the conventions already in the repository.
- Plan only as much as the task needs.
- Make the smallest change that satisfies the request; no speculative features.
- Verify the changed behavior with focused checks, then report what you ran and the
  real outcome.
- No automatic commits.

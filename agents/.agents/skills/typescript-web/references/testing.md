# TypeScript/web tests

Use repository scripts and framework.

- Unit test pure state/domain logic.
- Component test observable behavior and accessibility, not implementation internals.
- Integration test server/client contracts and runtime validation.
- Playwright smoke critical user flows and responsive breakpoints.
- Mock external boundaries, not the component under test.
- Run typecheck, lint, focused tests, and production build for routing/bundling changes.

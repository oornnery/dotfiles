# /etc/wsl.conf — managed via stow (dotfiles/wsl).
#
# After editing: `wsl --shutdown` from PowerShell, then reopen the distro.

[boot]
systemd=true

[user]
default=oornnery

[interop]
enabled=true
appendWindowsPath=true

[network]
generateHosts=true
generateResolvConf=true
